spring:
  rabbitmq:
    host: 192.168.64.140
    port: 5672
    username: admin
    password: admin
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/muzzmuzi/springcloud
          search-paths: /config
  application:
    name: config-server
eureka:
  client:
    service-url:
      defaultZone: http://eureka1:2001/eureka,http://eureka2:2002/eureka
server:
  port: 6001
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh

